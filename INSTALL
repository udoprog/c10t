Dependencies
------------
zlib
libpng (>= 1.2)
libboost_thread (>= 1.40)
pthread (through boost)

To install dependencies on Ubuntu:
$ sudo apt-get install cmake libpng12-dev zlib1g-dev libboost-thread1.40-dev libboost-thread1.40.0

Building Source
---------------
To build the source run:
$ cmake .
$ make

The c10t executable will be in the current directory.

Running
-------
To run:
$ ./c10t

To get a list of all options run:
$ ./c10t -h
